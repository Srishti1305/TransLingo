services:
  - type: web
    name: translingo
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    region: oregon
    plan: free
    autoDeploy: true
    runtime: python 3.10
    envVars:
      - key: FLASK_ENV
        value: production

